spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      uri: http://127.0.0.1:8080
      name: ${spring.application.name}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
